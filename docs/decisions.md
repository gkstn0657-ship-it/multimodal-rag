# 설계 결정 기록

형식: 배경 → 결정 → 대가(트레이드오프). 결정을 뒤집을 때는 항목을 지우지 말고 아래에 새 항목을 추가한다.

## D-01. 서빙 엔진: vLLM → Ollama (2026-09-14)

- 배경: 개발 머신이 Windows 11이고 WSL이 설치되어 있지 않다(관리자 권한 없음). vLLM은 Linux 전용이다. Ollama 0.34이 이미 설치되어 있고 CUDA를 네이티브로 쓴다.
- 결정: Ollama로 Qwen2.5-VL을 서빙한다. OpenAI 호환 엔드포인트(`/v1`)를 쓰므로 파이프라인 코드는 서빙 엔진에 의존하지 않는다.
- 대가: vLLM의 연속 배칭이 없어 대량 캡셔닝 처리량이 낮다. Ollama는 요청을 순차 처리하므로 인덱싱은 단일 스트림으로 진행한다.

## D-02. VLM 크기: 7B 선택, 컨텍스트 8k → 4k (2026-09-14)

- 배경: 계획은 RTX 4070 12GB를 가정했으나 실제 GPU는 RTX 4070 Laptop 8GB다. Qwen2.5-VL-7B Q4는 가중치 약 5.5GB, 비전 인코더 약 0.7GB, 8k 컨텍스트 KV 캐시 약 1GB로 합계 약 7.5GB라 8GB에 겨우 들어간다. 3B는 여유가 있지만 표·차트의 숫자 전사 품질이 7B보다 눈에 띄게 낮다.
- 결정: 품질을 우선해 7B를 쓴다. 대신 컨텍스트를 4k로 줄여 KV 캐시를 절반으로 낮추고 GPU 상주 가능성을 높인다. 페이지 1장 캡셔닝은 비전 토큰 약 1,200개와 출력 500토큰 안팎이라 4k로 충분하다.
- 대가:
  1. 답변 단계에서 원본 페이지 이미지를 여러 장 넣을 수 없다. 이미지 투입 상한을 2장으로 두고 나머지 이미지 출신 청크는 캡션 텍스트로 대체한다.
  2. 캡셔닝 중에는 임베더·리랭커를 GPU에 올리지 않는다. 인덱싱은 "VLM 캡셔닝 → VLM 언로드 → GPU 임베딩" 순서로 분리 실행한다. 서빙 시점에는 VLM만 GPU, 임베더·리랭커는 CPU.
  3. 7B가 일부 CPU로 밀리면 페이지당 캡셔닝이 20초에서 40초 이상으로 늘 수 있다. Phase 0에서 실측하고, 40초를 넘기면 3B를 재검토한다.

- **추가 (2026-09-14, Phase 1 스모크 테스트 중 발견):** SDS KoPub 코퍼스의 실제 페이지 이미지는 약 300DPI(2480x3505px)로,
  원본 그대로 VLM에 넣으면 비전 토큰만 4,148개로 4k 컨텍스트를 초과해 요청이 거부됐다(실측 에러: `exceed_context_size_error`).
  `servers/vlm_client.py`에서 이미지의 긴 변을 1,280px로 다운스케일하는 전처리를 추가해 해결했다.
  대가: 매우 작은 글씨나 미세한 차트 눈금은 다운스케일 후 VLM이 잘못 읽을 수 있다 — 캡션 숫자 정확도 점검(Phase 1 검증)에서
  이 해상도로도 표 숫자를 정확히 읽는지 별도로 확인한다.

## D-04. 라우팅 4갈래와 병렬 캡셔닝: 12시간 → 1시간 (2026-09-14)

- 배경: 500페이지 스모크 테스트에서 캡셔닝이 장당 평균 11.9초였다. 전체 코퍼스의 이미지 경로 3,707장(9.1%)을
  순차 처리하면 약 12시간. 목표는 2시간 이내.
- 실측:
  1. 이미지 경로 3,707장 중 OCR 텍스트가 100자 이상인 페이지 1,387장. 스캔된 본문 페이지라 OCR로 검색 가능하다.
  2. 텍스트·OCR 모두 20자 미만인 페이지 1,478장. 표본 84장 중 80장이 400px 썸네일 기준 잉크 비율 0.2% 미만인 백지.
  3. 캡션 시간은 출력 길이가 지배한다. 프리필 1,592토큰은 1~3초, 생성은 초당 약 30토큰. 549토큰 페이지는 17.6초.
  4. Ollama 병렬 요청: 4요청에서 장당 3.7초, 6요청에서 3.5초로 포화. 최대 VRAM 6.66GB / 8GB.
  5. 이미지를 1024px로 줄이면 프리필이 1,173토큰으로 줄지만 장당 0.3초 이득. 작은 글씨 판독 위험 대비 이득이 작아 1,280px 유지.
- 결정:
  1. 라우팅을 네 갈래로 나눈다. 텍스트 ≥100자 → TEXT. 아니면 OCR ≥100자 → OCR(VLM 생략). 아니면 잉크 <0.2% → BLANK(인덱싱 제외). 그 외 → IMAGE(VLM).
  2. 캡셔닝을 병렬 4로 돌리고 출력을 400토큰으로 제한한다.
  3. 캡션은 JSONL 캐시에 즉시 기록해 중단 후 재실행 시 이어간다. 페이지 단위 실패는 재시도 1회 후 OCR/텍스트로 대체하고 전체를 멈추지 않는다.
  4. OCR 경로 페이지도 원본 이미지를 디스크에 남겨, 답변 단계에서는 VLM이 스캔 원본을 직접 본다.
- 예상: VLM 대상 약 900장 × 3.7초 ≈ 1시간.
- 대가:
  1. OCR 경로로 보낸 1,387장 안에 표가 있으면 OCR이 행·열 구조를 평탄화한다. VLM 캡션이었다면 구조가 보존됐을 것이다.
     시간이 남으면 이 페이지들을 2차 캡셔닝하는 옵션을 남긴다.
  2. 백지 판정 임계값(0.2%)은 84장 표본 기준이다. 옅은 워터마크 페이지나 연한 도장만 있는 페이지가 백지로 분류될 수 있다.
  3. 출력 400토큰 제한으로 매우 큰 표는 뒷부분이 잘릴 수 있다.

## D-07. 캡셔닝 프롬프트: 표·수치 창작 방지 (2026-09-15)

- 배경: IMAGE 경로 997장 중 표 형식(`|`) 캡션 130장에서 10장을 무작위 표본으로 뽑아 원본 페이지 이미지와
  직접 대조했다. 5장을 상세 검토한 결과:
  - 광복군 포대(민간기록물) 사진: 페이지의 유일한 실제 정보인 사진 캡션 문구("2021 민간기록물 수집
    공모전 대상 - 광복군 포대(남궁승영 소장)")를 놓치고, 이미지에 없는 이름("南雲鐵", "张德生")과
    숫자("NO. 3613")를 창작했다.
  - 울산광역시 정보화 기본계획 표지: 제목과 기간(2024-2028)만 있는 표지인데 연도별 세부 계획 5행 표를
    통째로 창작했다.
  - 다문화사회통합전략 부록 구분지: "페이지는 비어 있다"고 쓴 바로 다음 줄에 존재하지 않는 표 2개를
    만들어냈다(자기모순).
  - 동구 성인지 통계: 실제 수치("4.6")는 정확히 옮겼으나 존재하지 않는 "증가 추세 그래프" 서술을 덧붙였다.
  - 파주스포츠센터 성과평가: 실제로는 번호 목록(①②③)인데 빈 칸투성이 표로 왜곡했다.
  - 표본 10장 중 5장 상세검토에서 2장 심각한 창작, 1장 자기모순, 1장 근거 없는 서술 추가, 1장 형식 왜곡.
- 원인: 캡셔닝 프롬프트("표는 마크다운으로 유지하라")가 표가 없는 페이지에도 적용되어, VLM이 형식을
  맞추려 내용을 지어내는 경향을 유발했다.
- 결정: `indexing/caption.py`의 `CAPTION_PROMPT`를 수정한다. 실제로 표·차트가 있을 때만 표를 만들고,
  안 보이는 값은 "수치 불명"/"판독 불가"로 쓰게 하며, 사진 속 캡션·낙서·도장 문구를 최우선으로 옮기게
  하고, 장식용 디자인 요소는 무시하게 했다. 파이프라인 구조는 건드리지 않았다(`docs/plan.md` Phase 1 원칙).
- 검증: 문제였던 5장에 새 프롬프트를 그대로 재적용해 전부 정상화됨을 확인(광복군 포대 캡션 정상 인식,
  창작된 표 전부 사라짐, 목록을 표로 왜곡하지 않음). 이후 997장 전체를 새 프롬프트로 재캡셔닝했다.
  기존 캡션은 `data/captions_v1_backup.jsonl`로 보관한다(비교 참고용, git 제외).
- 997장 전체 재캡셔닝 후 통계: "수치 불명/판독 불가" 포함 3.6%(36장, 남발 아님), 표 포함 비율은
  13.0%(130장)에서 1.4%(14장)로 급감, 캡션 길이 중앙값 209자→84자.
- 표 비율 급감이 정보 손실인지 확인하기 위해 v1에서 표였던 페이지 8장을 무작위로 뽑아 v2와 나란히
  대조했다. 결과: 정보 손실이 아니라 형식 변화였다.
  - 실제 수치(예: 48.7%/51.3%)는 v1·v2 동일하게 정확히 보존됨.
  - v1에서 처음 확인한 추가 창작 패턴 2건을 이 대조에서 발견: (1) 존재하지 않는 `imgur.com` 이미지
    URL을 표 안에 지어 넣음(2개 페이지에서 반복), (2) 시스템 프롬프트 지시문 자체("영문 테이블 헤더는
    그대로... 숫자는 그대로...")가 캡션 본문에 새어나옴. v2에서는 두 문제 모두 사라짐.
  - 단순 항목 목록(제목+번호 목록)을 억지로 마크다운 표로 포장하던 습관이 사라지고 평문 목록으로
    바뀐 것이 표 비율 급감의 주 원인이며, 내용 자체는 동일하게 유지됨.
  - 존재하지 않는 "차트 x축/y축/추세" 서술도 v2에서 사라짐.
- 대가: 페이지 제목을 영어로 임의 번역하는 새로운 사소한 습관이 1건 관찰됨(제목 전체 번역, 표 헤더
  번역 금지 지시와는 별개 사례). 정보 손실은 아니나 다음 점검 때 확인 필요.

## D-10. 생성 품질 채점 결과: 비교가 샘플링 편차에 묻힘 (2026-09-16)

- 배경: `eval/judge_rubric.md`를 사전등록한 뒤 15문항(text 5 · cross 5 · visual 5) x 2조건(ours: 원본 이미지
  투입 / baseline: 텍스트만)을 Claude Fable 5.1(이 세션)이 채점했다. 결과는 `eval/generation_report.json`.
- 집계: ours 평균 43.5점, baseline 48.1점. 유형별 text 45.2 vs 57.6, cross 40.8 vs 40.8, visual 44.4 vs 45.8.
  ours가 이긴 문항 5, baseline 7, 동률 3.
- 그러나 이 집계는 두 조건의 차이를 말해주지 않는다. `eval/gen_eval_image_usage.json`으로 확인한 결과
  **15문항 중 이미지가 실제로 VLM에 들어간 문항은 3개**뿐이고, 그중 정답 페이지 이미지가 들어간 건 1번
  하나다(12·15번은 5위 OCR 청크의 무관한 이미지). 나머지 12문항은 두 조건의 프롬프트가 글자 하나까지
  같다. 그런데도 점수가 평균 10.4점(최대 34점, 2번 문항 62 vs 96) 벌어졌다 — Ollama 기본 temperature(0.8)
  에서 나오는 순수 샘플링 편차다. 즉 단일 샘플 채점으로는 조건 간 5점 차이를 해석할 수 없다.
- 이미지가 실제로 들어간 3문항만 따로 보면:
  - 1번(정답 페이지가 IMAGE 경로, 이미지 = 정답 페이지): ours 80 vs baseline 73. 이미지가 도움됐다.
  - 12번(5위 OCR 청크의 무관 이미지 투입): ours 8 vs baseline 34. 무관한 이미지가 답을 크게 망쳤다
    (존재하지 않는 참고문헌 창작).
  - 15번(5위 OCR 무관 이미지): ours 71 vs baseline 60. 편차 범위 안.
- 채점에서 드러난 생성 단계 결함(조건과 무관하게 발생):
  1. **D-09의 500자 절단이 정답을 잘라냈다.** 8번(암보험 민원 통계 607/588/673건)은 수치가 청크
     1,238자 위치, 12번(한약재 면세 한도 300g·150g·3kg·6병)은 884~1,250자 위치에 있었다. 둘 다 정답
     페이지가 1위로 검색됐는데도 답을 못 냈다.
  2. **퇴행 출력.** 14번(정수기)은 두 조건 모두 `@@@@…`만 출력했다. 당시 점검에서는 이상 문자를 못 찾았으나
     (제어문자·제로폭 기준으로만 봤음), 이후 D-12에서 CJK 호환 한자 U+F9CA가 원인임을 확인했다.
  3. **자기모순 마감.** 3·4·6번에서 답을 써놓고 끝에 "문서에서 찾을 수 없습니다"를 덧붙였다.
     시스템 프롬프트의 기권 지시가 과잉 적용된다.
  4. **깨진 토큰.** "스트로미", "압구당시", "쿠알아이더 교육 기 hội" 같은 언어 혼입이 산발적으로 나온다.
  5. **차트 수치 접근 불가.** 11번(ODA 유상/무상 그래프)은 정답 페이지가 TEXT 경로라 이미지가 VLM에
     들어가지 않았고, 그래프 값(350 vs 210)은 추출 텍스트에 없다. 텍스트가 100자 이상이면 차트가 있어도
     이미지 경로로 가지 않는 라우팅(D-04)의 맹점이다.
- 결론: 이 15문항 채점은 "이미지 투입 효과"에 대해 n=1의 방향성 사례(1번)만 준다. 대신 생성 단계의
  구체적 결함 5가지를 찾아냈고, 그중 1·2·"무관 이미지" 문제는 D-11에서 바로 고친다.

## D-11. 답변 생성 수정: 동적 컨텍스트 예산, 이미지 상위 순위 제한, temperature 0 (2026-09-16)

- 배경: D-10의 결함 1(500자 절단), 12번 사례(무관 이미지), 샘플링 편차.
- 결정:
  1. 500자 고정 절단(D-09)을 폐기하고, 텍스트 글자 예산을 `num_ctx - 답변 예약(500) - 오버헤드(200)
     - 이미지당 1,500토큰 x 이미지 수`로 계산해 재랭킹 순서대로 채운다(글자/토큰 1.5 가정, 청크당 최대
     2,500자). 이미지가 없는 대부분의 질의에서 예산이 약 5,000자로 늘어난다.
  2. 이미지는 재랭킹 상위 2위 안의 청크만 투입한다(`answer_image_max_rank`).
  3. 답변 생성 temperature를 0으로 고정한다. 평가 재현성이 목적이며, 서빙 품질에도 해롭지 않다.
- 검증(`eval/gen_eval_refix_check.txt`): 글자 예산은 이미지 0장 5,094자 / 1장 2,844자 / 2장 594자.
  - 8번(암보험 민원 통계): 607·588·673건 3/3 적중. 500자 절단 때는 0/3이었다.
  - 12번(한약재 면세 한도): 300g·6병 적중, 녹용 150g은 여전히 누락하고 "500g까지 검역 후 면세"로
    부분 오답(정답은 검역 후 150g 면세, 500g까지는 과세 통관). 0점대에서 부분 정답으로 개선.
  - 1번(학교안전 보상 절차, 정답 이미지 투입): 핵심 절차·90일 기한 유지, 재심사 결정 기한(60일)까지 추가.
  - 14번(정수기): temperature 0에서도 두 번 모두 `@@@@…`(정확히 31자). 원인은 컨텍스트에 들어 있던
    CJK 호환 한자 U+F9CA 한 글자였다 — D-12 참고.
- 대가: temperature 0은 같은 질문에 항상 같은 답을 내므로, 퇴행 출력이 나오는 입력은 매번 퇴행한다.
  이미지를 상위 2위로 제한하면 3~5위에 있는 진짜 관련 이미지는 텍스트(캡션)로만 들어간다.
  이미지 2장을 넣으면 텍스트 예산이 594자로 줄어 사실상 이미지만으로 답하게 된다.
- 추가 관찰(2026-09-16): temperature 0이 완전한 재현성을 보장하지는 않는다. 14번 재생성에서 프롬프트가
  글자 하나까지 같은 두 호출(이미지 없음)이 다른 답을 냈다(하나는 분류 목록, 하나는 기권). Ollama가
  직전 요청의 프롬프트 캐시를 재사용하는지에 따라 수치 경로가 미세하게 달라져 greedy 선택이 갈리는 것으로
  보인다. 재현 가능한 평가를 원하면 호출마다 러너를 재시작하거나 같은 프롬프트를 여러 번 돌려 다수결을
  내야 한다.

## D-15. IMAGE 경로 임베딩 텍스트: 짧은 v2 캡션은 OCR보다 검색이 약함 → 전사 우선 캡션 + OCR (2026-09-16)

- 배경: SDS KoPub 라벨셋은 IMAGE 경로 정답이 1개뿐이라(D-08) 캡셔닝 효과를 재지 못했다. IMAGE 경로 997장 중
  200장을 뽑아 VLM이 페이지 이미지를 보고 질문 하나씩 쓰게 했다(`eval/synth_query_gen.py`, 필터 후 194건,
  `eval/synth_queries_image_pages.jsonl`). 정답은 그 페이지다. 질문 생성 모델이 캡셔너와 같은 Qwen2.5-VL이라
  캡션 저장소에 유리할 수 있는 조건이다.
- 실측(`eval/synth_query_report_variants.json`, IMAGE 997건의 임베딩 텍스트만 바꾼 저장소 비교):

  | 임베딩 텍스트 | Recall@1 | Recall@5 | Recall@10 | MRR |
  |---|---|---|---|---|
  | v2 캡션 (현재, D-07 보수적 프롬프트) | 26.3% | 45.9% | 53.6% | 0.350 |
  | OCR만 (`page.ocr or page.text`) | 32.0% | 52.1% | 57.2% | 0.397 |
  | v1 캡션 (D-07 이전, 장황·창작 있음) | 26.3% | 49.5% | 53.6% | 0.357 |
  | v2 캡션 + OCR | 27.8% | 45.4% | 52.1% | 0.361 |
  | v1 캡션 + OCR | 32.0% | 53.1% | 58.2% | 0.408 |

  문항별(v2 캡션 vs OCR): OCR 우세 46, 캡션 우세 32, 동률 116.
- 해석:
  1. 캡션에 유리한 조건에서도 **OCR 문자열이 v2 캡션을 이긴다.** IMAGE 경로 페이지의 OCR은 100자 미만의 짧은
     조각이지만 제목·기관명·연도처럼 질문이 그대로 재사용하는 글자를 담고 있다. v2 캡션은 창작을 막느라
     "표지입니다", "부록" 수준으로 짧아져(중앙값 84자) 그 글자들이 빠졌다. D-07의 대가가 여기서 드러났다.
  2. 장황한 v1 캡션이 v2보다 검색은 낫다. 그러나 v1은 없는 이름·숫자·URL을 만들어 냈으므로(D-07) 답변
     컨텍스트로 쓸 수 없다. "검색에 좋은 텍스트"와 "답변에 안전한 텍스트"가 갈린다.
  3. v2 캡션에 OCR을 붙여도 거의 안 오른다(MRR +0.011). 짧은 일반 문장이 OCR 신호를 희석한다.
  4. 캡션이 OCR을 이긴 사례는 OCR이 비어 있는 페이지(사진, 통계 구분지)였다. 캡션은 OCR이 못 읽는 페이지를
     메우는 역할로는 유효하다.
- 결정:
  1. 캡셔닝 프롬프트를 **전사 우선(v3)**으로 바꾼다(`indexing/caption.py`의 `CAPTION_PROMPT_V3`): 보이는 글자를
     전부 그대로 옮기고, 표는 행 단위 '항목: 값', 차트는 '범례: 수치'로, 해석은 마지막 '요약:' 한 문장으로만.
     창작 금지·판독 불가 표기는 D-07 그대로다. 검색 가능한 문자열과 창작 방지를 동시에 노린다.
  2. IMAGE 경로의 임베딩 텍스트를 **캡션 + OCR 합집합**으로 바꾼다(`indexing/embed_store.build_chunks`).
     OCR이 비어도 캡션이 남고, 캡션이 짧아도 OCR의 문자열이 남는다.
  3. 997장을 v3로 재캡셔닝(`indexing/recaption.py`, `data/captions_v3.jsonl`)한 뒤 같은 194문항으로 재측정한다.
     v1+OCR(53.1%)을 넘어야 성공이다.
- 대가: 전사 캡션은 길어져 캡셔닝 시간과 답변 컨텍스트 예산(D-11)을 더 쓴다. 표가 큰 페이지는 400토큰
  상한에서 잘릴 수 있다. 합성 질의는 같은 모델이 만들었으므로 절대 수치는 상한 추정치다 — 사람이 쓴 질의로는
  더 낮게 나올 수 있다.

## D-14. TEXT 경로 차트 페이지 감지: 저비용 픽셀 규칙으로는 불가 (부정 결과, 2026-09-16)

- 배경: 11번(ODA 유상·무상 그래프)처럼 텍스트가 100자 이상이라 TEXT 경로로 간 차트 페이지는 VLM이 이미지를
  못 보고, 그래프 값은 추출 텍스트에 없다(D-10 결함 5). 페이지 이미지에서 그림·표 영역을 감지해 image_path를
  붙이면 답변 단계에서 원본을 보여줄 수 있다는 가설로 저비용 규칙 두 가지를 재봤다.
- 실측 1 — 채색 픽셀 비율(400px 썸네일, 채도>40): TEXT 경로 표본 1,099장의 중앙값 0%, p90 6.2%. 5% 기준이면
  13%(약 4,800장)가 잡힌다. 잡힌 예시는 실제로 색 도표·사진 페이지였다. **그러나 11번 정답 페이지는 0.4%**로
  통과하지 못한다 — 회색조 선 그래프다(`eval/figure_detect/figure_detect_sample.txt`).
- 실측 2 — 긴 직선(축·격자) 감지: 600px에서는 11번 페이지가 `hlines=1`로 안 잡힌다. 11번이 잡히는 설정
  (1000px 이상, 어두움<200)에서는 TEXT 표본의 **53~61%**가 함께 잡혀 변별력이 없다. 밑줄·표 괘선·페이지
  테두리가 전부 걸린다(`eval/figure_detect/figure_detect_lines*.txt`).
- 실측 3 — 이 벤치마크에 대한 의미: SDS KoPub visual 유형 정답 페이지(TEXT 경로 156장) 중 채색>5%는 21장(13%),
  text 유형 정답 페이지는 10%, cross는 6%. visual 정답 페이지의 채색 중앙값은 0.6%다. 즉 "시각형" 질문의 정답
  페이지는 대부분 회색조 표·선 그래프라, 색상 규칙은 이 질문들을 거의 돕지 못한다(`eval/figure_detect/figure_detect_visual_gold.txt`).
- 결정: 픽셀 휴리스틱 기반 그림 감지는 도입하지 않는다. 절반 넘게 오탐하는 규칙으로 이미지를 붙이면 D-11의
  이미지 예산(2장, 텍스트 594자)만 소모한다.
- 다음 경로: (a) VLM에 "이 페이지에 차트·표가 있는가"를 묻는 분류 — 37,074장 × 약 2초 ≈ 20시간, 채색>2%·직선≥2
  같은 느슨한 프리필터로 후보를 30% 안팎으로 줄이면 6시간 규모. (b) 원본 PDF가 있다면 벡터 그림 객체를
  직접 뽑는 편이 정확하다 — SDS KoPub은 페이지 이미지만 제공하지만 `annotations.parquet`의 `down_url`로 원본
  PDF를 받을 수 있다. DART 2차 코퍼스는 PDF 원본이라 (b)가 곧바로 가능하다.

## D-13. 기권 문장 자기모순 수정 (2026-09-16)

- 배경: 채점 15문항 중 3·4·6번이 답을 다 써놓고 끝에 "문서에서 찾을 수 없습니다"를 덧붙였다(D-10 결함 3).
  시스템 프롬프트가 "근거가 없으면 '문서에서 찾을 수 없습니다'라고 답하라"였는데, 모델이 이를 "덧붙여도
  되는 문장"으로 읽었다.
- 결정: (1) 프롬프트를 "근거가 전혀 없을 때만, 다른 내용 없이 그 한 문장으로만 답하라. 답을 썼다면 그 문장을
  어디에도 덧붙이지 마라"로 바꿨다. (2) 후처리 `strip_contradictory_abstention`: 기권 문구 외 실질 내용이
  40자 이상이면 기권 문구가 든 줄만 지운다. 기권만 있는 답은 그대로 둔다. 단위 테스트 4건.
- 검증(`eval/gen_eval_abstain_check.txt`, temperature 0, D-11 예산 적용 상태): 3·4·6번 모두 자기모순 없이
  답만 나왔다. 6번은 "합성니코틴은 담배로 분류되지 않아 과세 대상이 아니다"까지 정답과 일치하게 바뀌었다
  (채점 당시에는 정반대로 답했음 — 프롬프트 변경과 컨텍스트 예산 확대가 함께 작용한 결과).
- 대가: 7번(검색 실패, 정답 문서가 top-5에 없음)은 채점 당시 baseline이 정상 기권했는데, 이제는 검색된
  다른 문서(외국인근로자 산재 연구)로 답을 채웠다. 기권 조건을 좁히면 검색이 빗나갔을 때 기권하지 않고
  무관한 근거로 답할 위험이 커진다. 검색 신뢰도(재랭킹 점수 하한)로 기권을 강제하는 장치가 다음 과제다.

## D-12. 퇴행 출력 `@@@@…`의 원인: CJK 호환 한자 U+F9CA, NFKC 정규화와 자동 회복 (2026-09-16)

- 배경: 14번(정수기) 답변이 두 조건·temperature 0·0.8 모두 정확히 31개의 `@`였다. 원인을 단계적으로 좁혔다.
  1. 입력 변형 11종(줄바꿈 정규화, top-1 청크만, 시스템 프롬프트 제거, 질문만, 무관 청크, 질문 표현 변경 등)이
     전부 퇴행했고, "냉장고란 무엇인가?" 같은 컨트롤 단문까지 퇴행했다(`eval/gen_eval_q14_probe*.txt`).
     텍스트 전용 `qwen2.5:7b-instruct`는 정상. 이 구간의 응답은 모두 `usage.prompt_tokens == 0`이었다 —
     모델이 프롬프트를 처리하지 않고 반환한 것이다. Ollama 로그에는 같은 시간대(10:59)에 GPU 디스커버리
     워치독 타임아웃과 "unable to refresh free memory" 경고, 그 전(10:25)에 `llama-server terminated
     (exit status 1)` 크래시가 있었다.
  2. `ollama stop` 후 재로드하면 컨트롤 단문은 전부 정상. 그러나 14번 전체 프롬프트는 재시작 직후에도
     두 번 연속 퇴행했다(`eval/gen_eval_q14_after_restart.txt`). 즉 서버 저하와 별개로 이 프롬프트에
     결정적 트리거가 있다.
  3. 러너를 매번 새로 띄우고 이분했다(`eval/gen_eval_q14_bisect*.txt`, `gen_eval_q14_normalize.txt`):
     질문 단독 정상, 정수기 질문 + 정상 컨텍스트 정상, **정수기 본문 앞 200자만 붙이면 중립 질문으로도
     퇴행**(이때는 prompt_tokens=232로 처리된 뒤 퇴행). 뒤 120자에 트리거가 있었고, 줄바꿈·한자(U+4E00~9FFF)·
     중점 제거는 소용없었으며 **NFKC 정규화만 통과**했다.
  4. NFKC로 바뀌는 글자는 하나였다: "유입수(流入水)"의 **流 = U+F9CA, CJK COMPATIBILITY IDEOGRAPH-F9CA**.
     일반 한자 流(U+6D41)의 호환 코드포인트다. 한자 제거 정규식이 호환 한자 블록(U+F900~FAFF)을 안 덮어서
     3번 실험에서 살아남았다. 코퍼스 전체에서 2회, 2페이지에만 등장한다.
- 해석: Ollama/llama.cpp의 qwen2.5vl 토크나이저 경로가 이 호환 한자를 처리하지 못해 모델이 고정 토큰
  (`@` 반복)을 내는 것으로 보인다. 한 번 발생하면 이후 요청까지 `prompt_tokens=0`으로 퇴행하는 오염 상태가
  이어질 수 있고(NaN이 KV/프롬프트 캐시에 남는 방식으로 추정), 러너 재시작으로 회복된다. 텍스트 전용 모델은
  같은 글자를 정상 처리했다.
- 결정:
  1. `servers/answer.py`: 질문과 컨텍스트 텍스트를 VLM에 넣기 전에 NFKC 정규화한다(`normalize_for_vlm`).
     검증: 같은 200자를 NFKC만 적용해 넣으면 정상 요약이 나온다.
  2. `servers/vlm_client.py`: 퇴행 감지(글자 종류 2개 이하 또는 한 글자 80% 초과, 또는 `prompt_tokens == 0`)
     시 `ollama stop`으로 모델을 재시작하고 한 번 재시도한다. 두 번째도 퇴행이면 예외 → API 503.
     캡셔닝과 답변 생성 모두 적용. 단위 테스트 `tests/test_vlm_client.py`, `tests/test_answer.py`.
- 영향: D-10의 14번 0점(두 조건)은 모델 품질이 아니라 이 결함이다. `eval/generation_report.json`에 재분류하고,
  NFKC 적용 후 재생성한 답(`eval/gen_eval_q14_after_nfkc.txt`)으로 다시 채점한다.
- 대가: NFKC는 전각 영숫자·호환 기호도 함께 바꾼다(예: ㎏→kg, ①→1). 검색 임베딩은 원문 그대로 쓰므로
  영향이 없고 VLM 입력에만 적용된다. 트리거 글자가 희귀해(2/39,458) 재발은 드물겟지만, 다른 호환 문자도 같은
  문제를 일으킬 수 있어 감지·재시작 경로는 유지한다.

## D-09. 답변 생성 컨텍스트도 4k를 초과함 (2026-09-16)

- 배경: Phase 3 생성 품질 평가 표본(15문항)을 만들다가 첫 문항에서 `exceed_context_size_error`
  (5,519토큰 vs 4,096 한도)로 실패했다. Phase 2 스모크 테스트(500페이지) 때는 재현되지 않았는데,
  전체 코퍼스 인덱싱 후 청크 텍스트가 더 길고(페이지당 최대 13,000자) top-5 청크를 그대로
  이어붙이면 컨텍스트가 넘친다.
- 결정: `servers/answer.py`의 `_build_context_text`에서 청크당 텍스트를 500자로 자른다
  (`settings.answer_context_max_chars_per_chunk`). 답변 생성 자체도 500토큰으로 제한한다
  (`settings.answer_max_tokens`). ctx는 8GB VRAM 제약(D-02) 때문에 그대로 4096 유지.
- 대가: 500자를 넘는 청크는 뒷부분이 잘려 답변 근거가 일부 누락될 수 있다. 특히 표가 큰
  페이지에서 앞부분 행만 보고 답할 위험이 있다 — Phase 3 생성 품질 채점에서 함께 확인한다.

## D-08. Phase 3 검색 평가: SDS KoPub 라벨셋으로는 캡셔닝 효과를 검증할 수 없음 (2026-09-15)

- 배경: SDS KoPub VDR 600문항으로 `eval/retrieval_eval.py`를 만들어 judge 없이 자동 평가했다
  (QA의 `id` 필드가 곧 정답 페이지임을 ground_truth 인덱스로 교차 검증).
  전체 결과: Recall@1 41.5%, Recall@5 77.0%, Recall@10 83.0%, MRR 0.564.
  유형별: visual이 가장 낮음(Recall@5 64.6%), text/cross는 80% 안팎.
- "캡셔닝이 실제로 도움이 되는가"를 보려고, IMAGE 경로 페이지를 캡션 대신 OCR/텍스트로 채운
  베이스라인 벡터 저장소(`data/vector_store_baseline_ocr`)를 만들어 같은 600문항으로 재평가했다.
  전체 지표가 우리 파이프라인과 거의 동일하게 나왔다(Recall@1 41.3% vs 41.5%).
- 원인 규명: 600문항 중 **정답 페이지가 실제로 IMAGE 경로인 문항은 1개뿐**이다(OCR 경로 9개,
  나머지 590개는 TEXT 경로). "visual" 유형 161문항 중에서도 160개는 이미 텍스트가 100자 이상
  추출되는 페이지였다 — 차트가 있어도 주변 설명 텍스트가 충분하면 TEXT 경로로 라우팅되기 때문이다.
  즉 두 저장소는 599/600 문항에서 사실상 동일한 텍스트를 인덱싱한 것이라, 전체 지표가 같은 건
  당연한 결과이지 "캡셔닝이 무의미하다"는 근거가 아니다.
- IMAGE·OCR 경로가 정답인 10문항만 따로 뽑아 재비교했다(`eval/ablation_caption_vs_ocr.json`):
  - OCR 경로 9문항은 우리·베이스라인 순위가 전부 동일함(두 저장소가 같은 OCR 텍스트를 쓰므로
    당연함 — 비교 로직이 올바르다는 확인이기도 하다).
  - 라벨셋에서 유일한 IMAGE 경로 정답 문항: 우리 파이프라인은 1위로 찾았고, 베이스라인은
    top-10 안에서도 찾지 못했다. n=1이라 일반화할 수 없지만 방향은 설계 의도와 정확히 일치한다.
- 결론: SDS KoPub VDR은 검색 알고리즘(재랭킹, 임베딩 등) 비교에는 유효하지만, 이 프로젝트의
  핵심 가설인 "VLM 캡셔닝이 순수 텍스트 추출보다 낫다"를 검증하기에는 라벨 커버리지가
  구조적으로 부족하다(전체의 0.17%만 IMAGE 경로). 이 사실을 감추지 않고 그대로 보고한다.
- 후속 조치(남은 일로 이관): IMAGE 경로 997장 자체를 대상으로 한 질의셋을 직접 만들거나
  (문서 제목·주변 맥락에서 질의를 뽑아 자체 라벨링), DART 2차 코퍼스에서 표·차트 밀도가 높은
  문서를 의도적으로 골라 질의를 구성해야 통계적으로 의미 있는 캡셔닝 효과 검증이 가능하다.

## D-03. 코퍼스: SDS KoPub VDR 1차, DART 2차 (2026-09-14)

- 근거와 대안 비교는 `docs/dataset.md`.
- 대가: 4만 페이지 전체를 캡셔닝할 수 없다(7B 기준 약 226시간). 텍스트 경로는 전체 인덱싱, 이미지 경로는 상한 3,000~5,000페이지로 제한한다. 상한 안에서 페이지를 고르는 규칙은 Phase 1에서 정한다.

## D-16. DART 2차 코퍼스: PDF 다운로드 0바이트 원인, 5개사 사업보고서 인덱싱 (2026-09-16)

- 배경: `OpenDartReader.attach_files()`가 주는 `pdf.do` URL을 그대로 다운로드하면 HTTP 200에 본문 0바이트가
  온다. 재현이 항상 동일해서 세션·인증 문제로 의심했다.
- 원인 규명: DART 사이트가 실제로 여는 첨부 다운로드 창은 `main.do`(팝업 HTML, 파일별 `<a href>`가
  결국 같은 `pdf.do` URL을 가리킴)이다. `main.do`를 먼저 방문하면 `PDFJSESSIONID` 쿠키가 발급되는데,
  이 쿠키만으로는 부족하고 **`pdf.do` 요청에 `Referer: <main.do URL>` 헤더가 있어야** 실제 PDF 바이트를
  준다(둘 다 빠지면 0바이트, 쿠키만 있고 Referer가 없어도 0바이트, 직접 확인). Referer 검사로 팝업을
  거치지 않은 요청을 막는 방식으로 보인다.
- 결정: `indexing/dart_fetch.py`에 `fetch_pdf_bytes()`를 추가해 `requests.Session`으로 `main.do` →
  `pdf.do`(Referer 첨부) 순서를 재현한다. `OpenDartReader`는 목록 조회(`list`, `attach_files`)에만 쓰고
  실제 바이트 다운로드는 이 함수로 대체했다.
- 수집 대상: 삼성전자·현대자동차·LG화학·NAVER·카카오(업종 분산), 사업연도 2022~2024 사업보고서
  (정정공시 포함 — 같은 회사의 여러 버전이 방해 문서 역할). 17개 PDF, 37MB, 8,418페이지.
- 인덱싱 결과(`indexing/run_index_dart.py`, `data/dart/index_report.json`): TEXT 8,326 / IMAGE 64 /
  BLANK 28, OCR 0(스캔 없음 — 모두 텍스트 레이어가 있는 born-digital PDF). IMAGE 64장은 D-15의
  `CAPTION_PROMPT_V3`(전사 우선)로 전량 캡셔닝 성공(실패 0), 8,390청크를 `data/vector_store_dart`에
  별도 저장(SDS KoPub 인덱스와 분리). 전체 14.6분.
- 캡션 샘플 점검: 표·확인서·결의안 등 본문을 전사 위주로 옮겼고 창작은 관찰되지 않음. 드물게 VLM
  오독(예: "담당"을 "당당"으로) 있으나 D-07이 막으려던 "없는 숫자·표 창작"과는 다른 종류의 오류.
- 대가/후속: DART는 SDS KoPub과 달리 스캔이 0%라 D-14가 문제 삼은 "차트 있는 TEXT 경로" 케이스를
  원본 PDF 벡터 그래픽 추출로 재시도해볼 실질적 대상이 된다(아직 미착수). 코퍼스 규모가 작아
  (8,418페이지) 자체 라벨링 질의셋을 만들기 전에는 검색 품질을 정량 평가할 수 없다 — 다음 단계로
  남겨둔다.

## D-17. VLM 호출을 OpenAI 호환 엔드포인트에서 Ollama 네이티브 API로 전환 (2026-09-16)

- 배경: v3 재캡셔닝 속도를 올리려다 `ollama ps`에서 모델이 **15% CPU / 85% GPU**로 갈려 있는 것을
  봤다(서버 로그 `offloaded 27/29 layers to GPU`). GPU 여유는 1.5GB인데 Ollama 추정기가 보수적으로 2층을
  CPU에 남긴 것이다. 또 캡셔닝 요청은 프롬프트 약 1,760토큰 + 출력 400토큰인데 컨텍스트는 4,096으로
  잡혀 KV 캐시(448MB)의 절반이 놀고 있었다.
- 시도: `num_gpu=99`, `num_ctx=3072`를 기존처럼 OpenAI SDK의 `extra_body={"options": ...}`로 보냈다.
  **아무 변화가 없었다.** curl로 격리 확인 — `/v1/chat/completions`에 `options`를 중첩으로 보내도,
  최상위로 보내도 무시된다. 같은 값을 네이티브 `/api/chat`에 보내면 즉시 반영된다(29/29 레이어,
  100% GPU, 모델 상주 6.4GB → 5.6GB, CONTEXT 3072). Ollama 0.34.0/0.34.1 동일.
- 결정: `servers/vlm_client.py`를 OpenAI SDK 대신 `requests`로 네이티브 `/api/chat`을 직접 호출하도록
  바꿨다. 메시지 형식(이미지는 `images: [base64]`), 출력 상한(`options.num_predict`), 퇴행 감지 신호
  (`prompt_eval_count == 0`, D-12)도 네이티브 형식으로 옮겼다. 공개 메서드 시그니처는 그대로라 호출부는
  바뀌지 않았다. 캡셔닝은 `caption_num_ctx=3072`, 답변 생성은 기존 4,096 유지(D-11 예산 계산 기준).
- 실측: 4병렬 처리량이 재시작 전 약 3.75초/장(표본 작음) → 이후 평균 3.2~3.4초/장. **10~15% 개선으로,
  기대(1.5~2배)보다 작다.** 병렬 슬롯이 2개(`OLLAMA_NUM_PARALLEL=2`)라 클라이언트 4스레드 중 2개는
  대기하고, 출력 길이(전사 캡션은 400토큰 상한에 자주 닿음)가 병목이라 CPU 2층 제거의 효과가 묻힌
  것으로 본다.
- 대가: D-01의 "OpenAI 호환이라 서빙 엔진을 바꿔도 클라이언트를 재사용한다"는 장점을 잃었다. 클라이언트가
  Ollama 전용이 됐다. 대신 GPU 배치·컨텍스트를 요청 단위로 제어할 수 있게 됐고, 이 제어가 없으면
  8GB에서 옵션 튜닝 자체가 불가능하다는 점이 더 중요하다고 판단했다. 남은 선택지(`OLLAMA_NUM_PARALLEL`
  3~4, KV 캐시 q8_0)는 Ollama 재시작이 필요해 이번엔 적용하지 않았다.

## D-18. 평가 방식: 실사용 질의셋 구축 대신 LLM-as-a-judge (2026-09-16)

- 배경: 두 자동 평가셋이 서로 반대 방향으로 편향돼 있다. SDS KoPub 600문항은 정답이 IMAGE 경로인 문항이
  1개뿐이고(D-08), 합성 194문항은 VLM이 페이지를 보고 만든 질문이라 페이지 안 어휘가 질의에 그대로
  들어가 검색이 실제보다 쉽다(D-15). 편향을 줄이는 정석은 실사용 형태의 질의셋을 만드는 것인데,
  개인 프로젝트라 실사용자가 없다. 진짜 질문 100개 수집 → 그 분포로 대량 생성 → 진짜 100개로 검증하는
  3층 설계를 검토했으나, 라벨링 비용이 남은 기간에 비해 크다고 판단했다.
- 결정: 검색·생성 품질 모두 판정 모델로 채점한다. 판정 모델은 생성 모델(Qwen2.5-VL-7B)과 다른 계열인
  Claude Fable 5.1이고, 이 세션 안에서 직접 채점한다(API 키·별도 스크립트 없음). 기준은 채점 전에 커밋한다
  — 생성은 `eval/judge_rubric.md`(기존), 검색은 `eval/retrieval_judge_rubric.md`(3등급 관련성, pooling,
  nDCG@5). 자동 지표에는 nDCG@k를 추가해(`eval/retrieval_eval.py`) 판정 결과와 같은 축으로 비교한다.
- 대가: (1) 판정 모델의 오류가 점수에 섞인다 — 표본 10%를 사람이 다시 봐 일치율을 남긴다. (2) 수동
  판정이라 표본이 40문항 안팎으로 작아 5점 이내 차이는 구분하지 못한다. (3) 질의 자체의 편향은 그대로
  남는다 — 판정은 "찾은 페이지가 맞느냐"를 고치는 것이고 "질의가 실사용자 같으냐"는 고치지 못한다.
  이 한계는 결과 보고에 항상 함께 쓴다.

## D-19. v3 캡션 결과와 검색 판정 평가: 세 시스템 동률, 실패 원인은 캡션이 아니었다 (2026-09-17)

- 자동 지표(합성 IMAGE 질의, 지시어 필터 보강 후 185건, `eval/synth_query_report_v3.json`):

  | 저장소 | R@1 | R@5 | R@10 | nDCG@5 | MRR |
  |---|---|---|---|---|---|
  | v3 캡션+OCR (D-15 결정) | 0.303 | 0.524 | 0.605 | 0.425 | 0.402 |
  | v2 캡션 단독 (운영) | 0.265 | 0.470 | 0.541 | 0.376 | 0.355 |
  | v1 캡션+OCR | 0.330 | 0.546 | 0.600 | 0.447 | 0.420 |
  | OCR 단독 | 0.319 | 0.530 | 0.584 | 0.427 | 0.400 |

  v3는 v2보다 확실히 낫지만(R@5 +5.4점) OCR 단독과 같고 v1+OCR에 2점 뒤진다(문항별 승 40·패 36·무 109 —
  잡음). SDS 600문항은 변화 없음(R@5 0.770, MRR 0.563, nDCG@5 0.609; 정답이 IMAGE 경로인 문항이 1개뿐이라 당연).
  전날 예상치(R@5 52~57%, MRR 0.40~0.44)의 하단에 걸렸다.
- 판정 평가(`eval/retrieval_judge_rubric.md`, 42문항, 풀 228페이지, Claude Fable 5.1이 텍스트 기준으로 등급):

  | 시스템 | SDS text/visual/cross(15) | SDS image(15) R@5(2등급) | image 무관@5 | image gold@5 | DART(10) R@5(2등급) | DART 무관@5 |
  |---|---|---|---|---|---|---|
  | v3 캡션+OCR | 세 시스템 동일 (R@5 1.0/0.8/1.0) | 0.27 | 0.41 | 0.80 | — | — |
  | v2 캡션 | 동일 | 0.27 | 0.41 | 0.73 | — | — |
  | OCR 단독 | 동일 | 0.27 | 0.44 | 0.80 | — | — |
  | DART 저장소 | — | — | — | — | 0.30 | 0.62 |

  풀 기준 nDCG@5(v3 0.928 / v2 0.894 / OCR 0.873)는 세 시스템의 top-5가 대부분 겹쳐 풀 안 순서만 반영하는
  포화 지표라 근거로 쓰지 않는다. text/visual/cross 15문항은 정답이 TEXT 경로라 세 저장소가 같은 텍스트를 갖고
  있어 결과가 같은 것이 맞다.
- **발견 1 — 합성 IMAGE 질의는 "찾아도 답이 없다".** gold 페이지를 top-5에 넣은 비율은 80%인데 그 페이지가 실제로
  질의에 답하는(2등급) 비율은 27%다. IMAGE 경로 페이지(텍스트·OCR 100자 미만)의 다수가 장 구분지·표지·발표
  제목 슬라이드라 내용이 없고, 합성 질의는 그 제목에서 만들어졌다. D-15에서 캡션 v2→v3 개선이 자동 지표에서
  OCR 단독과 같게 나온 이유도 이것이다 — 페이지에 옮길 글자가 몇 줄뿐이라 캡션 방식이 무엇이든 정보량이 같다.
  캡셔닝의 가치는 "내용 있는 시각 페이지"에서만 나오는데, 이 코퍼스의 IMAGE 경로엔 그런 페이지가 적다.
- **발견 2 — DART 실사용형 질의의 실패는 회사·연도 혼동.** 10문항 중 답을 찾은 3건은 개요 페이지에 답이 그대로
  있는 매출·영업이익·제품 비중. 실패 7건 중 "LG화학 주당 배당금"에 NAVER 배당 페이지 5개가 나오는 등 회사명이
  다른 페이지가 상위에 오르는 사례가 반복됐고, "NAVER 2022 최대주주"에는 2023·2024 보고서 페이지만 나왔다.
  밀집 임베딩이 "배당" 의미는 잡지만 "LG화학"이라는 고유명사 일치를 보장하지 않는 전형적 실패로, BM25 하이브리드와
  회사·연도 접두어(Contextual Retrieval의 축소판)가 정확히 겨냥하는 지점이다. DART 무관@5 0.62는 SDS(0.08~0.44)보다
  훨씬 높다 — 한 회사의 500페이지 보고서 5개사 3개년이 서로 방해 문서가 되는 설계가 의도대로 작동했다.
- 표본 설계 오류: 답 없는 문항으로 넣은 J42("삼성전자 2021년 배당 결정")는 2022 보고서의 전기 비교 열에 2021
  배당이 있어 실제로는 답이 있었다. 등급은 사실대로(2) 매겼고, 답 없는 문항 오탐 1/2은 이 문항이다. 남은 답 없는
  문항(J41 "카카오 2025 상반기 매출")은 오탐 0.
- 결정: (1) v3 캡션+OCR을 운영 저장소로 채택한다 — 자동 지표에서 v2보다 낫고 판정에서 무관 비율이 가장 낮다.
  v1과의 2점 차이는 잡음이고 v1은 창작 위험(D-07)이 있다. (2) 다음 개선은 캡션 프롬프트가 아니라 검색 단계다:
  BM25 하이브리드 → 회사·연도·문서명 접두어 순으로 DART 10문항과 합성 185문항에서 재측정한다. (3) 합성 IMAGE
  질의셋은 "gold를 찾았나"가 아니라 판정 2등급 기준으로만 해석한다.
- 대가: 판정은 페이지 텍스트(캡션·OCR·본문 앞 1,500자) 기준이고 이미지를 직접 보지 않았다. 표·차트 값이 이미지에만
  있는 페이지는 과소평가될 수 있다. 판정자 오류 점검(표본 10% 사람 재확인)은 아직 하지 않았다.
